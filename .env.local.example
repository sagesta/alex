# Local Docker Compose environment — copy this to .env.local and fill in values.
# Then run: docker compose --env-file .env.local up --build

# ── Clerk (required) ──────────────────────────────────────────────────────────
# Get these from https://dashboard.clerk.com → Your App → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
CLERK_JWKS_URL=https://YOUR-CLERK-INSTANCE.clerk.accounts.dev/.well-known/jwks.json
